spring:
  profiles:
    active: local # 기본 환경 선택
  main:
    web-application-type: servlet
#  jpa:
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

# local 환경
---
spring:
  profiles: local
  datasource:
#    data: classpath:data-h2.sql # 시작할때 실행시킬 script
    url: jdbc:postgresql://103.101.82.133:54311/bnkdevdb
    username: bnkdev
    password: bnkdev123
  jpa:
    show-sql: true
#    hibernate:
#      ddl-auto: create-drop
#  h2:
#    console:
#      enabled: true

# 운영 환경
---
spring.profiles: set1
spring.profiles.include: real-db

server:
  port: 8081

---
spring.profiles: set2
spring.profiles.include: real-db

server:
  port: 8082

